# Nova Labs Business 001 — Order Intake

This directory preserves the locked Mission 014C.2 benchmark bundle recovered from the prior run transcript.

## Preserved assets
- `catalog/mission_014c2_catalog.csv`
- `benchmark/mission_014c2_orders.csv`
- `ground_truth/mission_014c2_line_items.csv`

## Status
- Benchmark artifacts were not present in `/tmp` in this session.
- No persistent project repo was discovered through available filesystem tools.
- The benchmark was preserved locally under `/memories/`.
- Executable benchmark run was **not** completed in this session because the available sandbox/runtime connection was blocked by OAuth configuration.

## Notes
- These files are a recovered copy of the locked benchmark contents from the prior mission transcript.
- Ground truth should not be modified without separately documenting any defect.
